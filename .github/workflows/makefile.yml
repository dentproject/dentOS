name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docker-practice/actions-setup-docker@master
    - uses: actions/checkout@v3

    - name: build
      run: make docker
